/* eslint-disable no-unused-vars */
export {};
declare global {
  interface Window {
    erbs_msal_did_request_interaction_login: boolean; // this will be your variable name
    erbs_msal_did_request_token_redirect: boolean;
  }
}
